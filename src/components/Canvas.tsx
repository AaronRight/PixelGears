import React from 'react';

const style = {
    border: '1px solid',
    width: '600px',
    height: '800px',
};

function Canvas() {
  return (
    <div style={style} className="Canvas">
      123
    </div>
  );
}

export default Canvas;